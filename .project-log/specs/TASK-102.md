# TASK-102 恢复标签生成后端运行时版本

## 1. 问题

PC 端选择辅料并点击“生成标签记录”后提示：

```text
后端未返回可扫描的真实标签编号
```

## 2. 根因

- 前端已经包含 TASK-099 引入的 `label_payloads` 校验；
- `8011` 后端进程启动于 2026-09-13 14:55；
- `labels.py` 的 `label_payloads` 修改发生在进程启动之后；
- uvicorn 未启用 `--reload`，因此旧进程继续返回不含 `label_payloads` 的响应。

## 3. 维修动作

- 终止加载旧代码的 `8011` 进程；
- 使用当前源码在 `milk-backend` tmux 会话中重新启动 uvicorn；
- 验证健康接口版本、管理员登录和标签专项测试。

## 4. 完成条件

- `http://127.0.0.1:8011/api/v1/health` 返回 `version=1.0.4`；
- `tests/test_labels.py` 全部通过；
- 前端可直接重试标签生成，无需重新构建；
- 记录“源码更新后必须重启无 reload 后端”的操作经验。

权限级别：A。该维修只重启运行时，不修改业务数据、接口语义或代码。
