# TASK-096 历史任务状态收敛与跨端验证

## 1. 背景

任务台账中仍有 20 项早期 `implemented-unverified`。这些任务形成于项目早期，部分验证字段仍保留“尚未接入真实 API”“仍使用 MockRepository”等已经过时的限制。后续完成的任务已经覆盖其中大部分能力，但旧状态没有同步关闭，导致当前剩余工作清单失真。

本任务只收敛有充分当前证据支持的历史任务，不关闭仍依赖真实 Windows 11 安装或服务路径验证的项目。

## 2. 范围

- 核对 20 项历史未验证任务的完成条件和当前代码。
- 关联后续已完成任务及其真实设备、接口、构建和自动化测试证据。
- 将已经由当前实现和后续任务完整覆盖的项目标记为 `done`。
- 保留 `TASK-010/063/065/072` 的未完成状态，因为仍需要 Windows 11 真机安装、服务开机启动、多网卡或 ProgramData 自动备份验证。
- 更新早期任务中已经废弃的身份证规则描述，改为当前唯一工号规则。
- 不修改任何 PC、后端或 Android 业务代码。

## 3. 收敛清单

可以由当前实现和后续任务证据关闭：

`TASK-001/002/003/011/019/020/021/022/023/024/025/026/030/031/032/066`

必须保留：

`TASK-010/063/065/072`

## 4. 验证

- 后端完整测试通过。
- PC TypeScript/Vite 生产构建通过。
- Android Debug 构建和 Lint 通过。
- Android 15 模拟器 4 项仪器测试通过。
- 逐条映射文档与当前任务状态一致。
- Project Log、Loop 和差异检查通过。

## 5. 完成边界

本任务不替代 Windows 11 真机验收，也不把历史功能描述成所有现场异常均已验证。收敛后的剩余清单必须准确反映真实设备、客户规则和最终发布依赖。
