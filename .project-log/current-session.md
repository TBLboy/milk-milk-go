# Current Session

> 会话恢复入口。维护规则：
> - **头部快照**：顶部“当前状态”区块是稳定入口，每次更新时覆盖，不追加旧版本。
> - **最新在最上**：最新一次会话写在文件最上面的会话区块，旧会话依次向下。
> - **超限归档**：文件超过约 50-100 KB 或会话区块达到约 10 条时，把旧会话区块移动到 `.project-log/docs/archive/`，主文档只保留最近内容。
> - **单一事实源**：精确当前状态与下一步以 `.project-log/loop/handoff.md`、`.project-log/loop/active-run.yaml` 为准；不要在多份长文档里维护互相矛盾的“下一步”。
> - **机器文件不手工重排**：`loop/events.jsonl`、`loop/active-run.yaml`、`loop/handoff.md`、`verification/evidence.yaml` 由运行时维护，不做手工重排或改写。

## 当前状态

  - 当前阶段：implementation / TASK-109 注册页密码自动填充与可选工号已实现
  - 当前目标：完成剩余生产级可本地实现缺口，并在真实 Android 平板、Windows 11 和公司内部 WiFi 完成独立验收
  - 当前任务：TASK-109 已完成注册密码状态隔离、注册模式自动填充排除、可选工号字段、后端保存和唯一性校验；真实平板复验待执行
  - 当前状态：注册密码不再复用登录保存密码；注册页可填写可选工号，后端留空允许提交、审批前仍要求管理员补齐，填写后直接保存并检查唯一性
  - 下一步：下一次统一打包时重建 Android APK，在真实平板验证注册页密码框不会带入保存密码且工号可正常提交
  - 当前状态补记：TASK-103 已收口；PC 标签页选择辅料即显示 preview 预览且不落库，点击“打印”才创建正式标签记录并显示真实 `labelId`
  - 当前状态补记：用户反馈浏览器可扫码但 APP 长时间无法识别；已提高分析分辨率并接入 ML Kit 自动缩放，修复版本为 Android `1.0.5`
  - 当前状态补记：用户反馈正确二维码偶尔提示非本系统标签且镜头反复缩放；已区分 `preview=true` 的预览码，并限制自动缩放抖动，修复版本为 Android `1.0.6`
  - 当前状态补记：`TASK-097` 已生成 Windows `1.0.2` EXE 和 Android `1.0.2`（versionCode 3）APK；全新数据库安装会初始化 4 种辅料、2 个配方和 1 张演示工单
  - 当前状态补记：`TASK-098` 已允许 Android 服务器地址前两段在默认 `192.168` 基础上编辑，并生成 Android `1.0.3`（versionCode 4）APK；Windows 安装包保持 `1.0.2`
  - 当前状态补记：已核查 GPT 意见；用户确认真实标签打印机接入推迟到第一版 Demo 验收后，当前 Demo 只保留标签生成、二维码预览和记录；网络确定使用公司内部 WiFi，当前版本采用 HTTP，不引入证书和固定 IP 要求
  - 上一轮状态：TASK-068 功能审查稿和图标生成提示词已完成并通过敏感信息扫描；TASK-069 用户协议 V1.0 已完成
  - 当前状态补记：2026-09-13 确认平板连接失败根因是校园网客户端隔离，切换手机热点后可正常连接；针对完整 IPv4 输入的 1.0.1 修改已按用户要求撤销，恢复 Android 1.0.0 原有服务器设置方案
  - 待执行任务：讨论生产级整改优先级；TASK-065 Windows 11 真机复验；下一次统一打包时复核正式安装包和平板启动器图标
  - 当前状态补记：TASK-069 已完成协议正文、滚动弹窗和设备验证；正式交付前需由客户确认运营方法定名称、联系方式和法务意见
  - 当前状态补记：Q-023 已确认当前版本使用内部 WiFi HTTP 和手工主机 IP；HTTPS 仅保留为未来可选升级
  - 当前状态补记：TASK-065 已实现本机 IPv4 检测接口和 PC 设置页入口，优先推荐移动热点或物理网卡地址并排除虚拟网卡首选；后端 51 项测试、PC 构建、真实 8011 接口和 Chrome 页面交互均通过，待 Windows 11 真机复验
  - 当前状态补记：TASK-066 已完成平板顶栏底部分隔线和侧边栏内部展开/收缩按钮；Android 构建、模拟器交互和物理平板覆盖安装/冷启动验证均通过
  - 当前限制：TASK-070 仅完成核查和记录，未修改功能代码；确认缺口尚未转成正式实现任务；按用户要求 TASK-067 暂不重新生成正式 Windows 安装包和发布签名 APK
  - 当前状态补记：PC 管理端使用 `/auth/admin/login` 专用入口；前端恢复持久化会话时再次确认 `role=admin`，普通操作员会被明确拒绝并清理本地登录态
  - 当前状态补记：账号资料、注册审批和密码安全业务逻辑已固化到 REQ-001 v6，TASK-027 至 TASK-032 验证记录已回填；新增 TASK-033/TASK-034 等待平板和 PC 端到端人工验收；APP 注册按钮已修正为“提交注册申请”
  - 当前状态补记：APP 拍照/扫码闪退根因已修复，AndroidManifest 补充 CAMERA 权限并增加运行时授权和启动异常保护；TASK-035 已完成并通过 APK 构建，待真机复验
  - 当前状态补记：用户资料头像已支持选择/拍照后即时预览，保存前即可看到新头像；TASK-036 已完成并通过 APK 构建
  - 当前状态补记：用户资料页头像已支持点击查看放大图；TASK-037 已完成并通过 APK 构建
  - 当前状态补记：右上角头像菜单点击无响应已修复，去掉无回调时的禁用 Clickable 并让头像直接参与打开菜单；TASK-038 已完成并通过 APK 构建
  - 当前状态补记：APP 登录已支持记住最近账号和可选记住密码，账号记录保存到独立本地设置，退出登录后仍可下拉选择；TASK-039 已完成并通过 APK 构建
  - 当前状态补记：待审批工单未进入 PC 审批中心的 bug 已修复；TASK-040 已完成，后端把 `pending_approval` 工单返回给审批中心，PC 已接通批准/驳回，8011 已重启
  - 当前状态补记：APP 类型确认未扫码就自动通过的问题已修复，扫码结果不再预填当前步骤辅料 ID，并支持解析自制二维码 JSON；TASK-041 已完成并通过 APK 构建
  - 当前状态补记：辅料管理操作列已去掉“图片”和“停用”按钮，并移除状态列，构建通过
  - 当前状态补记：辅料编辑/删除 Not found 已修复，根因是 8011 后端未重启加载新接口
  - 当前状态补记：APP 登录页已增加“忘记密码”“用户协议”、注册页和登录态持久化；Android debug APK 使用 JBR 21 构建通过
  - 当前状态补记：平板登录页已删除管理员提示底行，将服务器地址移至“服务器设置”弹窗，并增加键盘弹起时的滚动与 imePadding 适配
  - 当前状态补记：平板登录页服务器设置改为登录框右上角齿轮图标；地址输入简化为只填写 `192.168` 后两段，端口和 `/api/v1/` 由系统固定补齐；IP 输入框改为固定窄宽度、居中对齐并在点号前后保留间距；Android debug 构建通过
  - 当前状态补记：服务器地址已从登录会话存储拆到独立 `milk_settings` 设置存储，退出登录不再清空，重启后仍会回填；Android debug 构建通过
  - 当前状态补记：进入头像、用户资料、右上角头像菜单的业务逻辑澄清；本轮按用户要求不修改代码
  - 当前状态补记：已确认头像作为后台账号资料跨平板同步；后台账号管理模块将扩展头像、身份证、电话等资料，下一步澄清维护权限与必填性
  - 当前状态补记：密码找回/重置方案正在澄清，建议采用“管理员仅重置为临时初始密码 + 首次登录强制修改密码 + 用户资料页凭当前密码自改密码”，不再允许管理员手动设置或查看用户密码
  - 当前状态补记：已确认管理员重置密码时由系统随机生成初始密码，弹窗一次性展示账号/新密码并支持一键复制，关闭后不可再次查看；平板注册账号沿用用户输入的密码，但提交后需后台审批，审批状态规则待最后确认
  - 当前状态补记：已完成账号资料/头像后台字段迁移、平板注册待审批、管理员随机重置密码、用户自助改密、PC 账号资料/审批/复制密码弹窗、APP 头像菜单/资料页/密码管理；后端测试、PC 构建、Android 构建均通过
  - 当前状态补记：生产流程照片证据已统一为应用内现场拍摄；类型确认只保留“拍照扫码”并从同一照片解析二维码；重量确认必须拍摄电子秤读数；照片统一添加操作员、时间、工单和步骤水印；TASK-042 已完成，待真机验收
  - 当前状态补记：TASK-043 已修复相机输出 URI 授权和读取链路；新 APK 已构建、升级安装并正常启动，待真实平板回归拍照扫码
  - 当前状态补记：TASK-044 已删除拍照后的水印提醒文案，实际水印处理逻辑保持不变
  - 当前状态补记：TASK-045 已完成 PC 工单详情证据图片点击放大；浏览器实测可显示原图并通过 Escape 关闭
  - 当前状态补记：TASK-046 已修复三步工单开始执行后申请按钮被挤出可视区域的问题；真机滚动到底部可操作三个申请按钮，后端 39 项测试和 Android 构建通过
  - 当前状态补记：TASK-047 已停用新的工单删除操作，平板工单列表支持分页；当前执行人自己的工单仅显示申请撤销，真实平板和后端回归均已验证
  - 当前状态补记：TASK-048 已将开始执行或提交完成与符合条件的申请按钮合并到同一行，真机已批准工单验证通过
  - 当前状态补记：TASK-049 已将工作台最近工单改为固定五项视口的内部滚动列表，真机验证五条完整显示且可滑动查看后续工单
  - 当前状态补记：TASK-050 已为工作台和工单页增加下拉刷新，真实平板验证加载指示器和后端数据请求均正常
  - 当前状态补记：TASK-051 已为 PC 端增加统一 5 秒自动同步与写操作即时刷新；审批红点和当前页面数据无需切换页面即可更新
- 已确认事实：客户需要防止辅料称重类型和重量错误；独立系统、不连接金蝶；第一版不接电子秤，称重结果手动输入并必须拍摄电子秤读数照片；后端运行 Windows 10/11；平板为 Android；电脑端浏览器管理端；主数据由用户维护并支持手工录入和 Excel 导入/导出；辅料和产品配方录入支持电脑和平板端但仅管理员可操作；辅料包含多张包装图片；现场首选平板、扫码枪次选；系统分电脑后端/数据库和平板 App，多平板并发，账号区分操作员；第一版以公司自制二维码为主识别方案；标签由后台管理员生成/打印，以 `printedAt` 为标签日期、独立 `labelId`、粘贴后扫码复核
- 活跃决策：Q-001/Q-002/Q-003/Q-005/Q-006/Q-007/Q-008/Q-009/Q-010/Q-011/Q-012/Q-013/Q-016/Q-017/Q-018/Q-019/Q-020/Q-021/Q-022 已确认；Q-004 已确认第一版不接电子秤；Q-014 供应商原码映射推迟；DEC-023/DEC-024/DEC-025/DEC-026/DEC-027/DEC-029/DEC-034 已生效；仓库类型：个人仓库
- 硬件查证：映拓计价秤公开资料曾显示疑似支持串口实时取重，但 DEC-023 已决定第一版不接电子秤，硬件研究标记为 superseded
- 技术研究：RES-001 电子秤串口查证标记为 superseded；RES-002 总体技术栈已完成并标记 complete
- 操作员工作流：真实产品制作辅料称量完整路径已梳理到 `.project-log/docs/operator-weighing-workflow.md`
- 称重证据：第一版手动称重必须同时保存数字重量和电子秤读数照片
- 待确认点：架构设计前输入已整理到 `.project-log/docs/solution-research-input-questions.md`
- 标签打印机调研：已完成，结论见 `.project-log/docs/printer-research-prc-2026-09-11.md`
  - 阻塞项：无
  - 最近验证：后端 47 项测试、PC 生产构建、Windows NSIS 安装器编译、APK v2/v3 签名校验和 `git diff --check` 通过
- 本轮架构和任务拆解产出：
  1. 增加后端领域服务层和文件证据服务边界
  2. 增加认证、主数据、工单、证据、导入导出、标签、健康运维接口分组及幂等契约
  3. 明确 users、materials、material_images、products、recipes、work_orders、work_order_steps、type_confirmations、weighing_attempts、evidence_files、approvals、labels/print_batches、settings、audit_logs、idempotency_records 等核心实体
  4. 明确工单/步骤/类型确认/称重尝试/打印批次状态和事务并发规则
  5. 明确照片先临时上传、业务提交后关联、打印未知状态人工确认、冲突刷新和失败恢复路径
  6. 已通过 `validate_project.py` 与 `loopctl validate`
  7. 已创建 DEC-028，确定 PC 前端优先开发
  8. 已拆分 TASK-001、TASK-002、TASK-003
  9. 已完成 PC 前端工程骨架、工作台、工单/审批、辅料/配方、标签、账号和设置页面
  10. 已完成桌面全导航和移动端菜单浏览器验证
  11. 当前开发服务器地址：`http://localhost:5173/`
  12. 后端开发服务地址：`http://127.0.0.1:8010/`，默认正式端口仍为 `8000`
  13. TASK-004 测试结果：2 passed，健康检查和 SQLite 文件初始化验证通过
  14. TASK-005 至 TASK-009 已完成；完整后端测试集当前 15 passed
  15. 已实现标签批次/Excel 校验、审计日志和数据库备份接口
- 下一步：
1. 在真实 Windows 11 电脑执行一次全新安装，确认服务注册、防火墙、浏览器管理端和 SMTP 配置自动生效
2. 覆盖安装一次，确认 `%ProgramData%\MilkWeigh\data` 中数据库和证据文件保留
3. 在真实 Android 平板安装发布签名 APK，回归登录、扫码、称重证据和 BUG 反馈
4. 补充安装后 SMTP 非阻塞测试、自动备份调度和 Windows 实机升级验证

## 2026-09-15 TASK-108 同名辅料扫码不一致诊断

- 用户扫描新白砂糖正式标签后仍提示与当前步骤不一致，排查确认不是扫码解析失败，而是系统身份 `materialId` 不同
- 当前 Demo 白砂糖为 `MAT-00001 / SWEET-01`，旧工单快照仍可能是 `MAT-00023 / A1`，两者中文名都为“精制白砂糖”
- 未放宽防错规则，仍严格比较 `materialId`；旧身份不会因中文名称相同而自动放行
- 后端不一致响应新增当前步骤、扫描标签和标签主数据的名称、内部代号及 `materialId`
- App 类型确认弹窗与扫码界面、PC 工单详情均显示当前步骤 `materialId`
- 新增同名不同 `materialId` 回归测试；后端专项 10 项、完整回归、PC 生产构建、Android Kotlin 编译和差异检查通过
- 本地 `8011` 已用当前源码重启，health 返回 `1.0.5`
- 证据：EV-TASK-108-FOCUSED、EV-TASK-108-BACKEND、EV-TASK-108-PC-BUILD、EV-TASK-108-ANDROID-COMPILE、EV-TASK-108-DIFF
- 限制：尚未重建 Windows EXE/Android APK，也未在真实工单完成用户复验

## 2026-09-15 TASK-107 Demo 主数据打包与发布文件重建

- 当前 SQLite 中的 14 种辅料、6 个产品和 6 个配方已固化为 Windows 安装器种子数据，共 29 条配方项
- 可可粉 `MAT-00014` 的 10 张包装图片已复制到安装器 payload，并由安装种子写入 `%ProgramData%\MilkWeigh\data\uploads`
- 种子按 `materialId`、产品名称和配方业务键幂等同步，不删除其他现有业务数据
- Windows 版本提升到 `1.0.5`，构建产物为 `发布版本/牧衡辅料称重防错系统-Windows-1.0.5-Setup.exe`
- Android `1.0.6` 与 Windows 包一起重新构建，`versionCode=7`，v2/v3 签名通过
- 验证：临时库和安装 payload 种子均为 14/6/29/10；重复执行无重复记录；完整后端测试、PC 构建、EXE/APK 哈希和签名校验通过
- 证据：EV-TASK-107-SEED、EV-TASK-107-PAYLOAD、EV-TASK-107-BACKEND、EV-TASK-107-WINDOWS、EV-TASK-107-ANDROID、EV-TASK-107-HASH
- 限制：尚未在真实 Windows 11 执行 1.0.5 全新安装或覆盖升级验收

## 2026-09-14 TASK-102 标签生成后端运行时恢复

- PC 前端已校验后端真实 `label_payloads`，但 `8011` 后端进程仍加载 TASK-099 修改前的代码，因此生成成功后响应缺少真实标签编号
- 终止旧进程并在 `milk-backend` tmux 会话中启动当前源码
- 验证：health 返回 `version=1.0.4`、管理员登录成功、标签专项测试 4 项通过
- 证据：EV-TASK-102-HEALTH、EV-TASK-102-LABEL-TESTS
- 限制：未在用户业务库中生成测试标签，页面重试结果由用户确认

## 2026-09-14 TASK-101 工单详情下拉刷新

- 工单详情页复用 `MainShell` 已有的 `isRefreshing` 和 `refreshDataWithFeedback()`，未新增后端接口或重复刷新状态
- 使用 Material 3 `PullToRefreshBox` 包裹详情页纵向滚动内容；刷新后由既有 `orders` 状态自动更新当前工单
- 验证：Android Debug 构建和 Lint 通过；差异检查通过
- 证据：EV-TASK-101-ANDROID-BUILD-LINT、EV-TASK-101-DIFF
- 限制：尚未在真实 Android 平板执行下拉手势和弱网反馈验证

## 2026-09-13 TASK-100 标签尺寸设置

- PC 系统设置新增“标签尺寸”，参数键为 `label_size_mm`，格式 `宽x高`，单位毫米，默认 `60x40`
- 服务端限制宽高均为 `10-300` 毫米，接受 `x`、`X` 或 `×`，统一保存为小写 `x`
- 标签预览按配置宽高比例显示；生成标签时把尺寸快照写入 `label_payloads[].labelSize`
- Windows 与后端版本提升到 `1.0.4`，生成 `牧衡辅料称重防错系统-Windows-1.0.4-Setup.exe`
- 验证：设置与标签专项测试 12 项、完整后端回归、PC 生产构建、Windows NSIS 构建、差异检查和安装包 SHA-256 校验通过
- 证据：EV-TASK-100-FOCUSED、EV-TASK-100-BACKEND、EV-TASK-100-PC-BUILD、EV-TASK-100-WINDOWS、EV-TASK-100-DIFF
- 限制：真实 Windows 11 覆盖安装和真实打印机纸张适配仍待现场验证

## 2026-09-13 TASK-099 PC 标签真实二维码修复

- PC 标签页原先将 `labelId` 硬编码为 `PREVIEW`，生成的是无法在后台校验的伪标签二维码
- `POST /labels/print-batches` 新增 `label_payloads`，返回与 `labels` 及数据库记录一致的真实二维码数据
- PC 端只在生成成功后构建二维码；生成前显示不可扫描占位提示，无真实标签时禁用放大预览
- Windows 与后端版本提升到 `1.0.3`，生成 `牧衡辅料称重防错系统-Windows-1.0.3-Setup.exe`
- 验证：标签与证据专项测试 12 项、完整后端回归、PC 生产构建、Windows NSIS 构建、差异检查和安装包 SHA-256 校验通过
- 证据：EV-TASK-099-FOCUSED、EV-TASK-099-BACKEND、EV-TASK-099-PC-BUILD、EV-TASK-099-WINDOWS、EV-TASK-099-DIFF
- 限制：尚未在 Windows 11 执行覆盖安装，也未使用真实平板扫描本轮生成标签

## 2026-09-13 TASK-094 Demo 标签生成语义与业务原子对齐

- 标签模块、导航、仪表盘和登录说明统一为“标签生成”，明确当前 Demo 只生成标签记录和二维码预览
- 页面移除“生成并打印标签”“打印完成后粘贴”“待打印”等会误认为已完成物理打印的语义
- 批次列表改为展示生成批次、生成人、生成时间和“已生成”状态；后端返回真实生成人
- `BL-LABEL-002` 已按 `DEC-037` 更新，真实打印机能力继续由 `TASK-086` 负责
- 对齐发现 `ALN-001` 已解决
- 验证：专项测试 10 项、后端完整回归 105 项、PC 构建、Playwright 生成链路、差异检查和项目校验通过
- 证据：EV-TASK-094-FOCUSED、EV-TASK-094-BACKEND、EV-TASK-094-PC-BUILD、EV-TASK-094-UI、EV-TASK-094-ALIGNMENT

## 2026-09-13 TASK-095 Android 照片 EXIF 安全加固

- Android Lint 发现旧 `android.media.ExifInterface` 存在已知安全缺陷，已切换为 `androidx.exifinterface:exifinterface:1.4.2`
- 保留现有照片方向枚举、旋转/镜像、压缩、水印和上传行为，不修改业务协议
- 验证：Android Debug 构建通过；Lint 通过并消除 ExifInterface 告警，0 errors；`git diff --check` 通过
- 证据：EV-TASK-095-BUILD、EV-TASK-095-LINT、EV-TASK-095-DIFF

## 2026-09-13 TASK-096 历史任务收敛与跨端验证

- 逐条审计 20 项历史 `implemented-unverified`，将 16 项已有当前实现或后续任务证据的任务收敛为 `done`
- `TASK-010/063/065/072` 保留未完成状态，统一等待 Windows 11 真机安装、服务、多网卡和自动备份验收
- 更新早期账号任务中的身份证描述为当前唯一工号规则，并收敛重复切换账号入口的历史说明
- 后端 105 项测试、PC 生产构建、Android Debug/Lint 和 Android 15 模拟器 4 项仪器测试全部通过
- 证据：EV-TASK-096-BACKEND、EV-TASK-096-PC-BUILD、EV-TASK-096-ANDROID-BUILD-LINT、EV-TASK-096-ANDROID-INSTRUMENTATION、EV-TASK-096-ALIGNMENT

## 2026-09-13 TASK-093 辅料主数据 Excel 导出

- PC 辅料管理页新增“导出辅料”，调用管理员专用 `GET /labels/excel-export`
- 导出文件 `materials-export.xlsx` 与现有导入模板字段一致，按内部代号排序，空辅料库也能生成带表头的有效工作簿
- 验证：专项测试 10 项、后端完整回归 105 项、PC 生产构建、浏览器下载、差异检查和项目校验通过
- 证据：EV-TASK-093-FOCUSED、EV-TASK-093-BACKEND、EV-TASK-093-PC-BUILD、EV-TASK-093-UI、EV-TASK-093-DIFF
- 限制：不实现 Excel 增量合并、覆盖导入或图片打包导出

## 2026-09-13 TASK-092 BUG 邮件状态、管理员自检和手动重试

- PC 系统设置页新增 BUG 反馈邮件面板，可查看最近记录、来源、提交人、图片数量、发送状态和脱敏失败原因
- 新增管理员专用的 SMTP 测试邮件、BUG 反馈列表和失败记录手动重试接口
- 重试使用数据库状态抢占避免重复发送；成功更新发送时间，原图片缺失时保留明确失败原因
- 验证：专项测试 8 项、后端完整回归 102 项、PC 生产构建、Playwright 交互、差异检查和项目校验通过
- 证据：EV-TASK-092-FOCUSED、EV-TASK-092-BACKEND、EV-TASK-092-PC-BUILD、EV-TASK-092-UI、EV-TASK-092-DIFF
- 限制：自动重试、在线替换 SMTP 凭据和真实客户网络投递不在本任务范围内

## 2026-09-13 TASK-091 Windows 安装后 SMTP 非阻塞自检

- 新增 `smtp_test.py`，从客户机 `%ProgramData%\MilkWeigh\config\.env` 读取配置并提交一封简短测试邮件
- 安装器在后台服务启动后以 15 秒上限执行自检；失败显示警告，不调用 `Abort`，不影响核心系统安装
- 自检成功或失败均写入 `%ProgramData%\MilkWeigh\logs\smtp-install-test.log`，日志对 SMTP 授权码、恢复密钥和 token secret 做脱敏
- 验证：聚焦测试 5 项、后端完整回归、临时 NSIS 编译、真实 SMTP 提交、脚本语法和项目校验均通过
- 证据：EV-TASK-091-FOCUSED、EV-TASK-091-BACKEND、EV-TASK-091-NSIS、EV-TASK-091-SMTP-REAL、EV-TASK-091-DIFF
- 限制：真实 Windows 11 客户网络中的安装告警、日志路径和 SMTP 提交仍由 TASK-083 验收

## 2026-09-13 TASK-090 Windows 覆盖升级配置保留修复

- 修复 NSIS 安装器无条件复制内置 `.env` 的问题：配置复制阶段临时关闭覆盖，完成 `.env` 和配置资源复制后恢复覆盖写入
- 首次安装仍会写入缺失配置；覆盖升级保留已有 `config\.env` 和其他已有配置文件，并继续补齐缺失文件
- 新增 `pc/server/tests/test_windows_installer.py`，静态约束 `SetOverwrite off -> 配置复制 -> SetOverwrite on` 的执行顺序
- 验证：聚焦测试 1 项、后端完整回归 93 项、临时 NSIS 安装器编译、`git diff --check`、`validate_project.py` 和 `loopctl validate` 均通过
- 证据：EV-TASK-090-STATIC、EV-TASK-090-NSIS、EV-TASK-090-SUITE
- 限制：真实 Windows 11 覆盖升级后的配置哈希和运行数据保留仍由 TASK-083 验收

## 2026-09-13 TASK-082 Android 1.0.1 真实平板升级验收

- HONOR ROL-W60（Android 15）先安装签名 1.0.0，配置服务器 `192.168.20.147`、记住账号与密码并登录工作台
- 使用 `adb install -r` 覆盖安装 1.0.1，设备显示 `versionCode=2`、`versionName=1.0.1`，首次安装时间保持不变
- 升级后服务器设置、账号、Android Keystore 加密凭据和登录会话均保留；退出后登录页仍回填账号和密码，重新登录成功
- 类型确认、称重录入、电子秤照片调用和 BUG 反馈入口均可打开；相机活动正常启动，应用进程无崩溃
- 为称重入口验证创建临时测试工单和标签，经正常 API 完成类型确认后通过申请撤销流程关闭；未推进正式业务数据
- 验证：APK 哈希和签名、ADB 升级、设备版本、UI 保留、接口、截图和日志检查通过
- 证据：EV-TASK-082-APK、EV-TASK-082-UPGRADE、EV-TASK-082-RETENTION、EV-TASK-082-SMOKE、EV-TASK-082-DIFF

## 2026-09-13 TASK-033 平板真机账号资料与密码闭环验收

- HONOR ROL-W60 真机使用重置后的临时密码登录后，强制进入首次修改密码弹窗；改密成功进入工作台
- 后端复核临时密码返回 401、新密码返回 200，`/auth/me` 返回工号 `QA-0913`、头像 `FILE-a03583f01dce2678eef5e155` 和 `must_change_password=false`
- 真机用户资料弹窗显示姓名、电话、工号和头像，头像为上传的红色郁金香图片
- 新注册账号待审批登录返回 403，管理员补充工号并批准后登录返回 200
- Android 模拟器登录同一账号后显示同一头像，验证头像跨设备同步；完全停止并重启后恢复工作台；退出登录后账号和加密保存的密码可回填并重新登录
- 验证：ADB UiAutomator 真机流程、模拟器跨设备流程、直接认证接口和 `git diff --check`
- 证据：EV-TASK-033-REAL-TABLET、EV-TASK-033-AUTH-API、EV-TASK-033-AVATAR-SYNC、EV-TASK-033-SESSION、EV-TASK-033-DIFF
- 限制：跨设备头像与重启恢复使用第二台 Android 模拟器；真机完成主要账号和密码操作

## 2026-09-13 TASK-034 PC 后台账号资料与审批闭环验收

- 将旧验收契约同步到当前唯一工号规则，移除已删除的身份证字段要求
- Chrome 实测新建账号头像、姓名、电话和工号；账号列表显示工号且不出现身份证
- 随机密码弹窗生成 10 位临时密码，复制内容与弹窗一致，关闭后不可再次查看
- 注册申请批准/驳回状态正确，待审批和已驳回账号登录均被后端拒绝
- 验证：账号专项测试 11 项、PC 生产构建、Playwright 闭环和项目日志校验通过
- 证据：EV-TASK-034-AUTH-TESTS、EV-TASK-034-PC-BUILD、EV-TASK-034-UI、EV-TASK-034-DIFF

## 2026-09-13 TASK-085 证据完整性检查管理界面

- PC 系统设置页增加管理员专用的证据完整性检查面板，接入现有 `/evidence/files/integrity-check` 接口
- 展示检查总数、正常、缺失、大小异常、哈希异常和历史未哈希数量；异常表包含文件编号、大小和 SHA-256
- 支持导出带 UTF-8 BOM 的异常 CSV；真实接口检查 37 个文件均正常，浏览器模拟异常后下载文件名正确
- 验证：后端 92 项测试、PC 生产构建、Playwright 管理员交互、`git diff --check` 和项目日志校验通过
- 证据：EV-TASK-085-PC-BUILD、EV-TASK-085-BACKEND、EV-TASK-085-UI、EV-TASK-085-DIFF
- 后续本地代码任务：无已批准 ready 项；TASK-082/083/084 等待真实设备或内部 WiFi，TASK-086/087/088 等待客户验收或决策

## 2026-09-13 GPT 整改项业务规则澄清

- 确认当前阶段不强制贴标后的二次复核扫码，保留 `labelId` 校验要求，并记录“正确标签贴到错误包装”的残余风险
- 确认一个产品只允许一个当前配方；配方影响生产配料的修改应保留不可变历史版本，历史工单继续使用原快照
- 确认删除身份证并替换为唯一工号；普通操作员工号必填且唯一，管理员账号工号可选
- 当前版本确定继续使用内部 WiFi HTTP，不引入 HTTPS、内部 CA、证书续期或固定 IP 要求
- 平板只需连接 WiFi 并手工填写后端电脑当前 IP；Q-023 已回答，Q-026 已延期
- 验证：`validate_project.py`、`loopctl validate` 和 `git diff --check` 均通过

## 2026-09-13 PC 与 APP 统一图标

- 将 `资源文件/app图标.png` 作为 PC 与 Android 的统一软件图标
- PC Web 接入 `app-icon.png` 和 `favicon.png`；Windows ICO 包含 16 至 256 像素共 9 个尺寸，NSIS 安装器、卸载器、桌面和开始菜单快捷方式统一使用 `app.ico`
- Android 接入 mdpi 至 xxxhdpi 普通、圆形和前景图标，并增加 Android 8.0+ 自适应图标与白色背景资源
- 验证：PC 生产构建通过；Android debug APK 构建通过且产物包含全部 `mipmap` 图标；脚本语法和 `git diff --check` 通过
- 证据：EV-TASK-067-ICON-PC、EV-TASK-067-ICON-ANDROID
- 限制：按用户要求未重新生成正式安装包，下一次统一打包时在 Windows 11 和真实平板复核最终显示

## 2026-09-13 GPT 功能审查意见核查

- 新增 `.project-log/docs/gpt-feature-review-verification-20260913.md`，逐项核查 GPT 返回的 P0/P1/P2 意见
- 确认标签打印未接入真实打印机，二维码确认未校验 `labelId`，自动备份没有调度执行，证据没有服务端哈希，HTTP、SQLite 并发和发布验证仍存在生产级缺口
- 确认超差称重尝试已经保存；人工读秤与照片一致性属于首版明确能力边界，不是隐藏实现缺陷
- 明确“辅料启用/停用”与客户已确认业务规则冲突，不纳入当前整改
- 补充审计日志覆盖范围不足、App 地址输入限定 `192.168` 网段等 GPT 未直接指出的问题
- 验证：敏感信息扫描、`git diff --check`、`validate_project.py` 和 `loopctl validate` 均通过
- 下一步：先讨论标签打印、`labelId` 校验和贴标复核闭环，再决定其它整改项的版本范围

## 2026-09-13 Demo 标签打印范围与内部 WiFi 补充

- 新增 DEC-037：当前 Demo 不连接真实标签打印机，只生成标签记录、labelId 和二维码预览；客户验收 Demo 后再实施真实打印机、打印状态、重打、作废和贴标复核
- REQ-001 更新到 v13，区分当前 Demo 的标签能力与后续正式打印能力
- 公司内部 WiFi 作为部署网络已确认，平板和后端处于同一局域网
- 新增 Q-023：内部 WiFi 是否接受 HTTP 明文传输，或需要升级 HTTPS 和内部证书方案，仍待确认
- 本轮仅更新范围、决策和开放问题，不修改功能代码

## 2026-09-13 功能审查与图标设计任务说明

- 产出 `.project-log/docs/milk-weigh-feature-review-and-icon-brief-20260913.md`，作为可直接发送给 GPT 的单文件任务说明
- 文档同时覆盖 PC 管理端、Android App、后端、角色权限、工单闭环、扫码与称重证据、审批、主数据、标签、账号、BUG 反馈、部署、安全、验证状态和已知缺口
- 明确区分已实现、已验证、未完成、待客户确认和需要 GPT 判断的事项，避免把代码现状误当成完整需求
- 第 14、15 节包含公司 Logo 分析、Windows/Android/Web 图标规格、4 个候选方向以及可直接复制的图像生成提示词
- 扫描确认文档不包含密码、恢复口令、SMTP 授权码或真实邮箱地址
- 验证：`git diff --check`、`validate_project.py` 和 `loopctl validate` 均通过
- 下一步：将 Markdown 与 `资源文件/公司logo.png` 一起发给 GPT，等待功能审查结论和 4 个图标候选

## 2026-09-13 APP 用户协议 V1.0

- 将登录页原先的两句简要提示替换为完整中文用户协议，标注版本 V1.0 和生效日期 2026年9月13日
- 协议覆盖账号申请审批、角色权限、现场操作、拍照扫码与称重证据、个人信息处理、长期留存与审计、禁止行为、知识产权、服务变更、责任边界、通知、协议更新和争议解决
- 协议弹窗改为受高度限制的可滚动正文区域，关闭按钮保持可见
- 登录和注册的协议勾选、错误提示及未同意拦截行为保持不变
- 验证：Android debug 构建成功；模拟器确认正文可滚动至第十三章、关闭返回登录、未勾选登录提示正确、无崩溃日志
- 证据：EV-TASK-069-BUILD、EV-TASK-069-UI；截图保存于 `.build/task069-*.png`
- 后续：正式交付前由客户确认运营方法定名称、联系方式和法务意见，再更新协议主体信息与版本

## 2026-09-12 1.0.0 发布包构建

- PC：生成 `牧衡辅料称重防错系统-Windows-1.0.0-Setup.exe`，内置 Windows Python 3.11、FastAPI、前端静态文件、pywin32 服务宿主、SMTP 配置和种子数据。
- PC 安装器：注册 `MilkWeighBackend` 自动启动服务，写入 `%ProgramData%\MilkWeigh\config\.env`，开放 TCP 8011，创建桌面和开始菜单入口；升级不删除数据目录。
- Android：生成 `牧衡辅料称重防错系统-Android-1.0.0.apk`，使用固定发布证书签名，APK Signature Scheme v2/v3 校验通过。
- 发布目录同时包含 `安装说明.txt`、单文件校验和与 `SHA256SUMS.txt`。
- 已知限制：当前构建机为 Linux，安装器已完成编译和结构检查，但尚未在真实 Windows 11 上执行安装；安装后 SMTP 测试和自动备份调度尚未实现。

## 2026-09-12 SMTP 授权码有效期与 Windows 安装一键配置调研

- 结论：QQ 免费邮箱没有公开承诺 SMTP 授权码永久有效；按“长期可用但可撤销凭据”管理
- 已确认风险：重新生成授权码、关闭 SMTP、开启邮箱登录保护、账号安全限制或腾讯策略变化都可能导致发信失效
- 推荐安装方式：安装向导输入一次发件邮箱和授权码，点击“测试并保存”，后端连接 `smtp.qq.com:465`、验证登录并提交测试邮件
- 推荐存储方式：授权码使用 Windows DPAPI 加密保存到 `%ProgramData%\MilkWeigh\config\`，生产环境不再使用明文 `.env`；升级保留配置
- 安全边界：机器范围 DPAPI 能防止明文和备份文件直接泄露，但不能抵御本机管理员提取凭据
- 兜底要求：BUG 反馈先落本地数据库，邮件失败保留状态并重试；后台增加测试邮件、替换授权码、失败告警和手动重试
- 零输入取舍：用户已接受安装包内置授权码可解包泄露的风险，第一版按零输入优先实现
- 真实联调：已使用 `732396482@qq.com` 的 SMTP 授权码完成 TLS 登录和测试邮件提交，SMTP 服务器已接受邮件
- 安全提醒：授权码已在聊天中出现，测试通过后应到 QQ 邮箱重新生成并替换本地配置
- 零输入安装：用户已确认安装包构建时从本机 `.env` 读取并使用 `732396482@qq.com`、`1218740205@qq.com` 和 SMTP 授权码，安装时自动落盘，不显示配置页；用户接受安装包内嵌凭据风险
- 后续任务：TASK-063 Windows 安装包内置 SMTP 配置并实现零输入部署
- 产出：`.project-log/docs/smtp-installer-research-20260912.md`、RES-003
- 未实现：安装向导、DPAPI 配置工具、后台邮件测试、凭据替换和 SMTP 失效重试

## 2026-09-12 PC 与 APP BUG 反馈及固定邮箱通知

- PC 顶栏在通知铃铛左侧增加 BUG 反馈图标；APP 在登录后主界面右下角增加 BUG 悬浮入口
- 两端均支持问题描述、最多 8 张 JPG/PNG/WebP 图片、缩略图预览、逐张删除和提交成功态
- 后端新增 `bug_reports` 表和 `POST /api/v1/bug-reports`，复用 `evidence/files` 上传并校验图片属于当前提交账号
- 邮件固定发送到 `1218740205@qq.com`；发件账号和 QQ 邮箱 SMTP 授权码只从被 Git 忽略的 `pc/server/.env` 读取
- 每次提交记录 `pending/sent/failed`、失败原因、时间并写审计日志；SMTP 未配置时明确返回 503，不伪造成功
- 验证：后端 47 项测试通过；PC 生产构建、Android debug APK 构建和 `git diff --check` 通过；Playwright 确认入口位于铃铛左侧且弹窗提交成功态正常
- 限制：真实邮件投递尚未执行，等待配置 `MILK_SMTP_USERNAME` 和 `MILK_SMTP_PASSWORD`

## 2026-09-12 优化标签预览对齐

- 右侧预览面板改为纵向 flex 布局，标签预览卡片通过自动外边距填充标题下方剩余空间
- 浏览器测量优化前卡片相对空白区域中心向上偏移约 26.75px
- 优化后垂直中心偏移为 0.5px，水平位置保持居中
- 验证：PC 生产构建通过，Playwright 页面测量和截图检查通过

## 2026-09-12 修复标签打印数量控件

- 根因：打印张数外层使用 `label` 包裹，浏览器会把标签内空白点击转发给第一个按钮，即减号
- 将只能展示的 `strong` 改为可编辑数值输入框，支持手动填写 1 至 10000 张
- 数量字段改为普通容器，加减按钮和打印按钮统一明确 `type="button"`
- 支持清空后自动恢复为 1、输入非数字时过滤、回车和失焦时规整数量
- 验证：PC 生产构建通过；Playwright 验证空白点击不变化、手动输入 137 保留、加减按钮正确、打印请求准确发送数量 37

## 2026-09-12 重建演示辅料和产品配方主数据

- 删除旧产品 `11`、`111`、`222`、`西番莲风味酸乳`、`高钙纯牛奶 1L`
- 删除旧辅料 `11`、`A1`、`B7`、`E2`、`F-02`
- 新建 14 个分类辅料，覆盖甜味剂、乳粉、稳定剂、乳化剂、矿物质、维生素、发酵剂、香精和风味原料
- 新建 6 个演示配方：高钙牛奶、原味酸奶、西番莲酸奶、草莓酸奶、低糖酸奶、巧克力牛奶
- 数据重建前备份保存于 `pc/data/backups/milk_weigh-20260912T134206Z.sqlite3`
- 演示清单：`.project-log/docs/demo-master-data-20260913.md`
- 验证：真实 8011 API 查询到 14 个辅料、6 个产品，每个配方包含 4 至 5 种辅料

## 2026-09-12 移除辅料启用停用隐藏业务逻辑

- 用户确认辅料管理从未需要启用/停用，之前出现的辅料停用状态属于历史字段和错误校验
- 移除辅料停用 API、PC 调用、App 状态展示和配方下拉筛选
- 配方保存只校验辅料是否存在和用量合法，标签打印只校验辅料是否存在
- 旧数据库 `enabled` 字段保留用于表结构兼容，但不再参与业务判断
- 验证：后端 44 项测试、PC 生产构建、Android debug APK 构建通过；真实 8011 材料接口已不返回 `enabled`

## 2026-09-12 已撤销：修复 APP 配方误选停用辅料导致保存失败

- 撤销原因：该修复基于未经确认的“辅料可停用”规则，与用户明确业务需求冲突

- 根因：App 辅料模型未读取后端 `enabled` 字段，配方下拉框包含已停用辅料；历史配方若仍含停用项，后端会拒绝整份配方
- App 辅料模型增加启用状态解析，辅料列表明确显示“启用”或“已停用”
- 配方下拉框只展示启用辅料；历史配方中的停用项显示“已停用”并在保存前提示具体需要替换的辅料
- 后端 `MATERIAL_NOT_AVAILABLE` 响应增加具体不可用 `material_ids`
- 验证：后端 43 项测试通过；Android debug APK 构建通过；模拟器验证产品 111 的停用辅料不再出现在下拉框，替换为 A1 后保存成功；测试后产品配方和管理员状态均已恢复

## 2026-09-12 APP 辅料配方表单滚动与图片管理完善

- 辅料和配方新增/编辑弹窗内容区增加受限高度和纵向滚动，较长配方可访问全部辅料与保存按钮
- 辅料列表增加“查看图片”，可横向查看包装图片并点击单张放大；无图片时入口禁用
- 辅料编辑弹窗展示已有图片和本次选择图片，支持新增、逐张删除和放大预览
- 配方新增/编辑支持可选产品图片的选择、预览、更换、删除和上传，配方列表显示产品图片
- 平板端新增通用远程/本地图片加载组件，复用现有证据文件接口读取图片
- 需求基线更新到 REQ-001 v10，明确产品图片为可选资料且不参与配方计算
- 验证：Android debug APK 构建通过；平板模拟器验证辅料两张图片查看与放大、已有配方图片展示、三辅料编辑弹窗滚动后完整访问；无 FATAL EXCEPTION

## 2026-09-12 PC 管理员恢复弹窗移除密码显示控件

- 用户要求管理员紧急恢复界面不显示密码眼睛按钮
- 恢复密码输入框增加独立样式类，并隐藏浏览器原生密码显示、清除、凭证自动填充和文本装饰控件
- 验证：PC TypeScript 和 Vite 生产构建通过；Chrome 打开恢复弹窗并聚焦输入框后截图，无眼睛控件，读取输入类型仍为 `password`

## 2026-09-12 PC 管理员紧急密码恢复

- PC 登录框右上角存在不可见热点，3 秒内连续点击 10 次打开“管理员紧急恢复”
- 恢复口令只提交到后端，后端与本机 PBKDF2 哈希比对；明文不进入前端、响应或审计日志
- 连续输错 5 次锁定 15 分钟，成功、失败和锁定均写审计日志
- 恢复成功后生成 10 位随机临时密码，并在弹窗中一次性显示和支持复制
- 管理员旧令牌立即失效，使用临时密码登录后必须修改密码
- 本地哈希配置保存在被 Git 忽略的 `pc/server/.env`
- 验证：后端 43 项测试通过；PC 生产构建通过；浏览器实测隐藏入口和恢复成功；验证前已快照管理员密码哈希和认证版本，验证后完整恢复

## 2026-09-12 PC 顶栏用户菜单与本人账号管理

- PC 右上角头像和向下箭头支持点击展开下拉菜单
- 菜单包含用户信息、密码管理和退出登录，并支持点击外部或 Escape 关闭
- 用户信息弹窗复用 `/auth/me`，支持查看账号、角色和身份证，修改姓名、电话和头像
- 保存用户信息后顶栏头像、姓名和本地会话立即更新
- 密码管理复用 `/auth/me/change-password`，必须验证当前密码，新密码至少 8 位且两次输入一致
- 验证：PC 生产构建通过；后端 41 项测试通过；临时管理员成功修改资料和密码，旧密码被拒绝，新密码可登录；临时账号已删除

## 2026-09-12 PC 管理端普通操作员越权登录修复

- 用户反馈普通操作员账号可以登录 PC 管理端，严重违反已确认的角色边界
- 根因：PC 与平板共用 `/auth/login`；该接口为支持平板而允许普通操作员登录，PC 前端只判断登录成功，没有校验管理员角色
- 后端新增管理员专用 `POST /api/v1/auth/admin/login`，普通操作员返回 `403 ADMIN_PORTAL_REQUIRED`
- PC 登录切换到管理员专用接口；本地会话恢复和 `/auth/me` 结果再次校验管理员角色，非管理员会清理登录态
- 平板端继续使用 `/auth/login`，普通操作员登录行为未被收紧
- 验证：后端 41 项测试通过；PC 生产构建通过；浏览器使用普通操作员账号仍停留在“管理员登录”并显示“普通操作员账号不能登录电脑管理端”；测试账号已从真实数据库清理

## 2026-09-12 PC 端业务数据自动同步

- 新增统一数据同步事件和自动刷新 Hook
- 所有写操作成功后立即触发本地数据同步，本机审批红点即时更新
- 侧栏审批数量、工作台、工单、审批、辅料、配方、标签、账号设置和工单详情均接入自动刷新
- 其他客户端产生的变化通过 5 秒短轮询同步；页面隐藏时暂停，恢复焦点时立即刷新
- 静默刷新保留当前数据和加载状态，网络短暂失败不清空页面
- 验证：PC 生产构建通过；浏览器自动化验证跨客户端变化和本机审批即时刷新均正常

## 2026-09-12 APP 主页面下拉刷新

- 工作台和工单两个主页面接入 Material 3 下拉刷新容器
- 下拉后重新请求工单列表和产品主数据，刷新期间显示加载指示器并阻止重复触发
- 工单页改为整页 `LazyColumn`，标题、筛选区和列表空白均可参与下拉手势
- 刷新失败时保留当前数据并显示网络提示
- 验证：Android debug APK 构建通过；真实平板两页均显示刷新指示器；后端收到新的工单与产品请求；无崩溃日志

## 2026-09-12 工作台最近工单五项滚动列表

- 最近工单区域固定显示五个紧凑工单行
- 超过五条工单时在列表内部上下滚动
- 工单行保留产品、执行人、工单号、进度、更新时间和状态
- 验证：Android debug APK 构建通过；真机五条完整显示，向上滑动后可查看后续工单

## 2026-09-12 合并工单状态与申请操作按钮

- 已批准工单同一行显示“开始执行”和“申请撤销”
- 执行中工单按规则显示提交完成或状态提示，不显示开始执行
- 申请接管仅在非执行人、非创建人且无待审批申请时显示
- 后台已停用的删除操作不恢复
- 验证：Android debug APK 构建通过；真机两个操作按钮纵向位置一致

## 2026-09-12 工单操作收敛与平板分页

- 保留“申请撤销”作为唯一工单停止操作，停用新的删除申请和后台删除接口
- 历史已删除数据仅做兼容展示，平板筛选移除“已删除”
- 平板工单列表按每页 8 条分页，支持上一页、下一页和总数展示
- 平板读取 `operator_id` 和 `created_by`，当前执行人或创建人不再看到申请接管
- 验证：后端 40 项测试通过；PC 构建和 Android debug APK 构建通过
- 真机验证：工单列表显示“第 1 / 2 页 · 共 9 条”并可切换第二页；执行人自己的三步工单只显示“申请撤销”

## 2026-09-12 修复相机拍摄后无法读取照片

- 用户反馈：拍照扫码一直提示“照片处理失败，无法读取拍摄照片”
- 根因风险：AndroidX 默认 `TakePicture` 契约没有显式给外部相机应用授予输出 URI 的读写权限；部分设备即使返回拍摄成功，APP 随后通过 ContentResolver 再打开该 URI 也可能失败
- 修复：改用自定义相机契约，对 `EXTRA_OUTPUT` URI 添加读写授权并设置 ClipData
- 修复：相机目标文件路径使用 `rememberSaveable` 保存，避免相机页面返回时 Activity 重建导致路径丢失
- 修复：二维码解析、EXIF 方向读取和水印处理改为直接读取 APP 缓存中的目标文件，不再二次通过 ContentResolver 打开
- 验证：Android debug APK 构建成功；APK 升级安装成功；MainActivity 正常启动
- 待办：在出现问题的真实 Android 平板上重新安装 APK，分别验证拍照扫码、无码拍照申请和称重读数拍照
- 后续调整：删除“已自动添加操作员姓名和时间水印”等显式提醒；称重照片说明改为“现场拍摄”，处理中提示不再强调水印。Android debug APK 构建及升级安装通过

## 2026-09-12 PC 工单详情证据图片点击放大

- 用户要求：后台工单详情中每个步骤的拍摄图片可点击弹出放大图，只显示图片，不增加文字说明
- 实现：`EvidenceThumb` 增加点击预览和 Portal 全屏遮罩，使用原文件地址显示放大图
- 交互：点击遮罩或按 `Escape` 关闭；打开期间锁定页面滚动
- 验证：`npm run build` 通过；在 Chrome 中打开工单 `WO-20260912084531-496`，详情检测到 3 张证据缩略图，点击后放大层正常出现，原图尺寸 `3264×2448`，按 `Escape` 可关闭

## 2026-09-12 修复多步骤工单申请操作不可见

- 用户反馈：三步配方工单开始执行后找不到申请接管、撤销、删除入口；一至两步工单正常
- 根因判断：申请按钮位于所有内容底部，三步工单时超出平板固定可视区域，且详情页原先没有纵向滚动，并非工单状态或后端接口限制
- 修复：申请按钮继续保留在详情内容底部，工单详情页整体增加纵向滚动
- 回归：新增三步工单开始执行后提交撤销申请的后端测试
- 验证：后端 39 项测试通过；Android debug APK 构建通过
- 真机验证：在 Android 平板打开三步执行中工单 `WO-20260912095043-860`，向上滚动后底部完整显示“申请接管 / 申请撤销 / 申请删除”；点击“申请撤销”正常打开提交窗口

## 2026-09-12 统一现场拍照证据与类型拍照扫码

- 用户要求：类型确认融合为“拍照扫码”；拍摄后自动解析二维码、添加操作员和时间等水印并上传；除类型确认外，重量确认等所有生产证据照片也只能拍照，不能选择相册
- 实现：类型确认弹窗移除相册、独立扫码和手工辅料 ID 入口，拍摄照片先在本地解析二维码，再添加操作员、时间、工单和步骤/辅料水印
- 实现：二维码匹配成功时，将同一张带水印照片绑定到类型确认记录；未识别二维码时，可用同一张现场照片填写原因并提交后台拍照审批
- 实现：重量确认弹窗移除相册入口，必须拍摄并上传带水印的电子秤读数照片后才能提交
- 后端：类型确认的 `evidence_file_id` 改为必填并校验由当前操作员上传；照片申请同样校验上传人
- 稳定性：相机照片按 EXIF 方向归一化后再解析二维码和生成水印，减少平板旋转照片识别失败
- 验证：后端 38 项测试通过；Android debug APK 构建通过；APK 已在 Android 模拟器安装并启动，登录页正常渲染且无崩溃；8011 后端已重启，健康检查返回 ok
- 待办：在真实 Android 平板上验收相机授权、照片方向、水印观感、二维码拍摄识别、无码审批和称重照片上传

## 2026-09-12 APP 登录记住账号与记住密码

- 用户要求：平板登录页增加记住账号功能，记录用户登录过的账号；同时增加记住密码复选框，勾选后登录成功记住密码
- 实现：`SessionStore` 增加独立 `remembered_logins` 本地记录，最多保存 5 个最近登录账号；退出登录只清理当前会话，不会清空已记住账号与密码
- UI：账号输入框右侧在存在记录时显示下拉箭头，可选择最近账号并回填对应密码；登录区默认记录账号，只保留“记住密码”复选框和“用户协议”勾选；下拉菜单提供“清除已记账号”
- 验证：`JAVA_HOME=/home/tbl/android-studio/jbr ./gradlew assembleDebug --no-daemon` 构建通过
- 待办：重新安装 APK 到真机，确认账号下拉、密码自动回填和清除记录体验
- 后续调整：登录按钮下方入口合并为“忘记密码 / 帐号注册 / 用户协议”一行，原“没有账号？注册普通账号”改为“帐号注册”；注册页底部保留“返回登录 / 用户协议”
- 再调整：去掉“记住账号”复选框，账号默认记录；登录页只保留“记住密码”和“用户协议”两个勾选项
- 再调整：登录页“记住密码”与“用户协议”勾选并排显示以节省空间；注册页继续单独保留用户协议勾选
- 再调整：用户头像菜单中“切换账号”与“退出登录”实际行为重复，已移除“切换账号”，保留“退出登录”；切换账号可通过登录页已记住账号下拉完成
- 2026-09-12 待审批工单审批中心修复：平板创建工单后状态为 `pending_approval`，但 `/approvals` 原先只查注册、拍照和工单请求，未查工单本身；已在 `/approvals` 增加 `work_order` 类型，PC 批准映射到 `/work-orders/{order_no}/approve`，驳回映射到 `/cancel`；后端测试 37 passed、PC 构建通过、8011 已重启
- 2026-09-12 APP 类型确认修复：扫码结果输入框原先初始化为当前步骤的 `materialId`，导致不扫码也能通过；现在打开弹窗时为空，扫码后从自制二维码 JSON 中提取 `materialId`，未扫描到 ID 时禁止确认并提示
- 2026-09-12 APP 扫码回调补充修复：ZXing 结果解析原先传入 requestCode `0`，与 `IntentIntegrator.REQUEST_CODE` 不匹配，导致正确或错误二维码返回后都没有回填；已改为使用 `IntentIntegrator.REQUEST_CODE`，Android debug APK 构建通过

## 2026-09-12 右上角头像菜单点击修复

- 用户反馈：点击右上角头像没反应，不出现下拉列表
- 修复：头像组件不再在无点击回调时保留禁用态 Clickable，避免遮挡外层 `IconButton`；同时头像自身也直接触发打开菜单
- 验证：Android debug APK 构建通过

## 2026-09-12 用户资料头像放大预览

- 用户反馈：进入个人资料后，希望点击头像直接查看放大图
- 实现：`AvatarImage` 增加可选点击回调；资料页头像可点击，弹出 280dp 放大预览；后台头像与新选择/拍摄头像均可放大查看
- 验证：Android debug APK 构建通过

## 2026-09-12 用户资料头像即时预览

- 用户反馈：选择新头像或拍照头像后，头像位置不跟随变化，必须点击保存才刷新
- 修复：`AvatarImage` 增加本地 `previewUri` 支持，用户资料弹窗把 `selectedAvatarUri` 传入预览；选相册或拍照返回后立即显示新图片，保存成功后仍上传并同步后台头像
- 验证：Android debug APK 构建通过

## 2026-09-12 平板相机权限闪退修复

- 用户反馈：点击 APP 拍照功能直接闪退
- 根因：AndroidManifest 缺少 `CAMERA` 权限，也没做运行时授权；扫码和拍照入口直接启动相机，权限缺失或设备异常时崩掉
- 修复：补充 `android.permission.CAMERA` 和可选 camera feature；新增统一 `cameraPermissionLauncher`，点击时先检查并请求权限；头像拍照、扫码、无码拍照、电子秤读数拍照都改为安全启动，失败时显示提示而不是崩溃
- 验证：Android debug APK 构建通过；`aapt dump permissions` 确认 APK 包含 `android.permission.CAMERA`
- 待办：重新安装 APK 到真机，验证首次授权弹窗、扫码和拍照

## 2026-09-12 账号资料与密码业务逻辑固化和任务拆解

- 用户确认头像、资料维护权限、平板注册审批、随机重置密码、首登强制改密和自助改密流程
- 已把该业务规则同步到 REQ-001 v6 和 BL-AUTH-002 至 BL-AUTH-005
- 已回填 TASK-027 至 TASK-032 实现与验证记录，状态与当前代码一致
- 已新增 TASK-033 平板真机账号/密码验收、TASK-034 PC 后台账号/审批验收，等待人工端到端执行
- APP 注册按钮由“注册并登录”改为“提交注册申请”，避免与待审批业务冲突

## 2026-09-12 账号资料、注册审批与密码安全落地

- 用户要求：先固化账号资料、头像菜单、平板注册审批和密码重置业务逻辑，再拆任务并开始落地代码
- 业务固化：头像作为后台资料跨平板同步；身份证/电话由管理员维护并脱敏；平板注册账号先待审批；管理员只可随机重置一次性初始密码；用户凭当前密码自助改密
- 后端新增：用户表 `status/avatar_file_id/phone/id_card` 字段和轻量迁移；`/auth/users` 管理员创建账号、`/auth/me` 资料更新、`/auth/me/change-password` 自助改密、随机重置密码、注册账号审批接口；审批中心新增账号注册申请
- PC 新增：账号列表头像/电话/身份证/状态；新建/编辑账号资料；随机密码一次性弹窗和一键复制；账号注册批准/驳回；审批中心支持新账号注册申请
- APP 新增：注册提交后等待审批不再自动登录；右上角关闭按钮替换为头像菜单；用户资料页支持头像、姓名、电话；密码管理支持当前密码+新密码；首次登录强制修改密码
- 验证：`pytest -q` 36 passed；`npm run build` 通过；`JAVA_HOME=/home/tbl/android-studio/jbr ./gradlew assembleDebug --no-daemon` 通过；`http://127.0.0.1:8011/api/v1/health` 返回 ok
- 已知限制：安平台真机相机、头像跨设备同步、注册审批完整联调需真机验证

## 2026-09-12 平板登录页服务器设置体验优化

- 用户要求：服务器设置入口不要文字按钮，改为登录框右上角齿轮图标；打开的系统设置弹窗后续可继续扩展其他配置
- 地址输入改为极简模式：界面固定展示 `192.168.`，只填写第三段和第四段；使用数字键盘并限制最多三位数字；保存时校验空值和 `0..255`
- 保存后自动拼写完整地址 `http://192.168.x.y:8011/api/v1/`
- 用户反馈输入框仍太长，已将第三段、第四段输入框从等宽拉伸改为固定 82dp 窄框，并让整行在设置弹窗中居中
- 用户反馈点号前后间距不足，已为整行地址组件统一增加 8dp 间距，使 `192.168.`、输入框和点号之间不再贴在一起
- 用户反馈 IP 地址无法持久保存；根因是服务器地址原先存在 `milk_session` 中，退出登录执行 `clear()` 时会一起清空
- 修复：`SessionStore` 新增独立 `milk_settings` SharedPreferences 保存服务器地址；`clear()` 只清理登录会话，不再影响服务器配置；同时保留旧 `milk_session` 地址读取兼容
- 验证：`JAVA_HOME=/home/tbl/android-studio/jbr ./gradlew assembleDebug --no-daemon` 通过
- 下一步：继续平板真机联调与 PC 端剩余验收

## 2026-09-12 PC 审批链路与平板真实接口接入

- 用户要求：Windows 安装包暂时不需要，先把 PC 端和平板端整体写好调试完成
- PC 端新增工单接管、撤销、删除申请：
  - 新增 `WorkOrderRequest` 表和 `pc/server/app/api/work_order_requests.py`
  - 平板/操作员提交申请，后台审批通过后分别执行接管、撤销、软删除
  - 删除工单使用 `deleted` 状态保留历史记录，操作员列表隐藏已删除工单，管理员仍可追溯
  - 审批中心现在同时列出拍照放行、接管、撤销、删除四类待审批申请
  - PC 工单详情展示申请记录，工单列表支持“已删除”筛选
- 平板端从 `MockMilkRepository` 切到 `RealMilkRepository`：
  - 登录、注册、工单、主数据、创建/开始/完成工单、二维码确认、拍照审批、称重、申请接管/撤销/删除均调用 FastAPI
  - 图片上传改为真实 multipart 文件上传，包装照片与电子秤读数照片均可选相册或拍照
  - 二维码扫描接入 ZXing 扫描界面，登录页支持保存后端地址，普通操作员也可读取启用产品创建工单
  - 管理员拍照审批从“模拟审批”改为真实后台审批接口
- 验证：后端 34 项测试通过；`npm run build` 通过；`JAVA_HOME=/home/tbl/android-studio/jbr ./gradlew assembleDebug --no-daemon` 通过；`http://127.0.0.1:8011/api/v1/health` 返回 ok；PC 开发地址 `http://192.168.20.147:5173/`
- 已知限制：平板真机扫码、相机权限、局域网连通、图片上传和完整工单流转尚未真机验证；Windows 安装包任务按用户要求延后

## 2026-09-12 APP 工单详情、类型确认与称重 Mock 流程

- 用户要求：继续推进 Android App 与 Windows 部署相关剩余任务
- 上一轮已把 APP 工单步骤模型落入 AppData.kt，本轮修复详情页总是打开第一条工单的问题：MainShell 增加 `selectedOrderNo`，工单列表、工作台最近工单、新建工单都会跳转到对应工单详情
- TASK-022：详情页展示产品、目标重量、执行人、整体进度和辅料步骤；步骤状态细化为待确认/待审批/待称重/已完成
- TASK-023：当前待确认步骤弹出类型确认弹窗，支持模拟扫码辅料 ID、相册选择包装照片、填写放行原因、提交拍照申请、模拟后台审批通过
- TASK-024：待称重步骤弹出称重弹窗，支持手动输入读数、必须选择电子秤读数照片、允差通过后完成步骤、超差提示重新称重
- TASK-025：工单详情增加申请接管、申请撤销、申请删除入口，提交原因后展示待审批状态，状态保存在 MockRepository
- 构建验证：`JAVA_HOME=/home/tbl/android-studio/jbr ./gradlew assembleDebug --no-daemon` 通过
- 已知限制：当前为 MockRepository，真实二维码摄像头扫码、摄像头拍摄、图片上传和 /evidence 联调未完成；接管/撤销/删除后端审批接口尚未实现；真实审批需要后台管理员操作
- 下一步：继续补齐 TASK-026 平板管理员主数据 Mock；Windows 安装包任务按用户要求暂时延后

## 2026-09-12 APP 管理员主数据 Mock

- 用户要求：Windows 安装包任务暂时不需要，先把整台平板端和 PC 端写好调试完成，之后再补 Windows 安装包任务
- 已先提交上一轮 APP 工单详情、类型确认、称重和申请审批改动：`5bca031 feat(app): complete work order steps and request mock flows`
- TASK-026：管理员导航内的“辅料与配方”从占位页改为真实 Mock 页面
  - 辅料列表支持新增、编辑，字段包括内部代号、中文名称、英文名称、保质期和相册多选包装图片
  - 产品配方列表支持新增、编辑，从现有辅料库选择辅料并填写每吨用量，支持启停状态和移除/添加辅料
  - MockRepository 新增材料库和配方保存；新建工单现在按产品配方明细生成辅料步骤并换算目标重量
  - 普通操作员导航中仍不显示“辅料与配方”
- 构建验证：`JAVA_HOME=/home/tbl/android-studio/jbr ./gradlew assembleDebug --no-daemon` 通过
- 已知限制：当前为 MockRepository，真实主数据 API、图片上传、摄像头拍摄和真机验证未完成
- 下一步：整体检查 PC 端页面、按钮和真实后端接口联调，再处理平板真实 API 接入和 Windows 安装包

## 2026-09-12 APP 会话、注册、工单创建与列表优化

- 用户要求：查看剩余任务并继续推进；已确认剩余任务集中在 Android App 与 Windows 部署
- TASK-020：登录页增加“注册普通账号”切换与注册表单，注册成功后自动登录；MockRepository 登录/注册返回 token；新增 SessionStore 使用 SharedPreferences 持久化用户与 token，App 启动恢复登录态，退出清除
- TASK-021：新增产品 Mock 数据与创建工单 Repository 方法；工作台和工单列表增加“新建工单”入口；新建工单弹窗支持产品下拉选择、目标生产重量输入和创建待审批工单；工单列表增加状态筛选、已撤销排序到末尾、每页 8 条分页占位
- 构建验证：`JAVA_HOME=/home/tbl/android-studio/jbr ./gradlew assembleDebug --no-daemon` 通过
- 已知限制：当前为 MockRepository，真实后端登录/工单接口联调未完成；真机运行验证未执行

## 2026-09-11 APP 登录页忘记密码与用户协议

- 用户反馈：优化登录界面，增加“忘记密码”和“用户协议”功能
- 实现：登录卡片内增加“忘记密码”和“用户协议”入口；“忘记密码”弹出提示联系系统管理员重置；“用户协议”弹出简要协议说明
- 实现：登录按钮上方增加“我已阅读并同意《用户协议》”勾选，未勾选时点击登录提示先阅读并同意协议
- 验证：源码修改完成；按用户要求，当前环境不执行构建验证，需由用户在 Android Studio 中确认编译

## 2026-09-11 辅料编辑/删除 Not found 修复

- 用户反馈：辅料编辑和删除都提示 Not found
- 根因：后端 8011 服务启动于 11:04，而 `master_data.py` 编辑/删除接口修改于 11:19，运行中的旧进程没有加载新路由
- 修复：重启 `milk-backend` tmux 会话，重新启动 uvicorn 8011
- 验证：真实 HTTP 接口创建临时辅料 `MAT-00007`、PUT 编辑、DELETE 删除均返回 200，临时数据已删除；前端 5173 返回 200

## 2026-09-11 得力 DL-720W 查证

- 用户反馈：候选打印机是得力 DL-720W，商家描述为“可以通过 USB 连接电脑打印”
- 查证：得力官方用户手册确认 DL-720W(NEW) 支持 `TSPL`、`QR Code`、`USB B-Type`、缝标/黑标侦测、最大打印宽度 80 mm、Windows 7/8/10/11/XP 驱动
- 结论：商家一句话不足以下最终结论，但官方手册已覆盖“Windows 本机 + TSPL + USB + 二维码”的核心必要条件；Android/树莓派 USB 通道、TSPL 指令手册和实际标签打样仍需确认
- 记录：详细调研已补入 `.project-log/docs/printer-research-prc-2026-09-11.md`

## 2026-09-11 工单详情状态样式统一

- 用户反馈：工单详情里的“工单状态”彩色背景与周围详情卡片背景不一致，显得突兀
- 修复：在 `.detail-summary strong .status` 中移除状态徽章背景色和内边距，只保留状态文字和小圆点的语义颜色
- 验证：`npm run build` 通过

## 2026-09-11 APP 启动前业务与技术回顾

- 结论：REQ-001 已批准、Android 原生 Kotlin/APK 技术方向已确认，可以开始 APP 开发，但应按现有后端接口覆盖范围分批推进
- 已具备：登录/普通账号注册、管理员主数据、工单创建/详情/审批/开始/完成、图片上传、二维码匹配、拍照申请审批、手动重量+读数照片提交、自制二维码 JSON 内容格式
- 缺口：平板查看其他工单当前能力不足；接管申请/审批未实现；操作员发起撤销/删除并后台审批未实现；APP 列表分页/状态筛选后端未实现；关键提交 requestId 幂等未实现
- 建议：第一轮先搭建 Android 工程、登录、工单列表/详情、扫码/拍照/称重核心流程；接管和撤销/删除页面放到后端补齐接口后再开发

## 2026-09-11 APP 前端任务拆解

- 已新增 TASK-019 至 TASK-026，覆盖 Android 工程骨架、登录注册、工作台工单、工单详情步骤、类型确认、重量确认、接管/撤销删除申请、管理员主数据前端
- 开发策略：先开发平板前端页面并使用 MockRepository 预留真实 API 契约，UI 验收后再开发/联调后端
- 当前未写任何 APP 代码，仅更新 `.project-log/tasks/task-list.yaml` 和会话记录
- 结构校验：新增任务 YAML 可解析且字段符合任务清单 schema；完整校验器仍提示任务清单中既有 `implemented-verified` 枚举兼容问题，未改动原有任务记录

## 2026-09-11 APP 目录结构整理

- 用户确认：`app` 根目录作为 Android Studio 工程根，不强制拆分 `backend` 和 `front`
- 已迁移：将原先 `app/front` 下的 Gradle 工程文件上移到 `app/`，形成 Android Studio 可直接识别的 `app/settings.gradle.kts`、`app/build.gradle.kts`、`app/app/src/main/...` 结构
- 已移除：`app/backend` 和 `app/front` 旧目录已移出项目，源码入口统一为 `app/app`

## 2026-09-11 APP 登录页背景图接入

- 用户提供 `资源文件/app主页.png`，确认用作 Android 登录页背景
- 已复制到 `app/app/src/main/res/drawable-nodpi/app_main_bg.png`
- 已在 `LoginScreen` 中作为整页背景图使用 `ContentScale.Crop`，并增加浅色白色覆盖层保证标题和登录卡片可读
- 已整理背景图生成简报：`.project-log/docs/app-login-background-brief.md`
- 验证：当前沙箱无法通过 Gradle Wrapper 写入用户目录，未在本环境重新构建；需在 Android Studio 中执行一次编译确认

## 2026-09-11 APP 登录输入框提示样式优化

- 用户反馈：点击账号/密码输入框后，浮动标签会遮挡输入框边线，交互不必要
- 修复：将账号和密码 `OutlinedTextField` 的 `label` 改为内部 `placeholder`，聚焦后不再显示上浮小框，也不改变登录逻辑
- 验证：源码修改完成；当前环境 Gradle Wrapper 仍因用户级缓存目录只读未能重新构建，需在 Android Studio 中验证

## 2026-09-11 APP 前端第一批落地

- 已完成 TASK-019 的代码落地：在 `app` 建立 Android Kotlin + Jetpack Compose 工程、MockMilkRepository、登录壳、角色导航、工作台、工单列表/详情占位和管理员入口
- 已预留：`MilkRepository` 接口、`BuildConfig.BASE_URL`、后续真实 API 替换边界
- 构建验证受环境限制：当前 Gradle 离线缓存缺少 Android Gradle Plugin 模块元数据，无法完成 `assembleDebug`，暂标记 `implemented-unverified`
- 未推进：真实后端、摄像头、二维码扫描、照片选择和重量提交；后续按 TASK-020 至 TASK-026 继续

## 2026-09-11 辅料管理操作列精简

- 用户反馈：辅料列表右侧“图片”按钮和“停用”按钮多余，辅料不需要停用/启用，直接删除即可
- 已修改：辅料表格移除“状态”列，移除右侧“图片”按钮和“停用”按钮；保留包装图片数量入口、编辑和删除
- 验证：`npm run build` 通过

## 2026-09-11 标签打印机自动打印深度调研

- 结论：国内桌面级热敏标签打印机可通过 TSPL/TSPL2 原生指令或厂商 SDK 实现 PC 自动生成二维码并批量打印；直接热敏不干胶模切标签纸可直接粘贴
- 佳博：公开 C# Windows 示例工程使用官方 DLL，支持 USB、状态查询、文本和 QRCODE 指令，并已有本机 HTTP 8848 打印服务实现
- 汉印：官方产品页和下载中心确认标签打印机、模切/连续标签纸耗材、驱动/SDK/工具/手册下载分类
- 芯烨：TSPL/TSPL2 标签机，Windows 工具可经 winspool 原始打印队列发送 RAW TSPL；官方 SDK 有 Python ctypes 包装示例并支持二维码
- 推荐集成：本机打印助手接收后端 HTTP 打印请求，发送 TSPL 原始指令；浏览器纯打印不合适
- 未决：最终机型、接口、标签尺寸、二维码中心 logo 识别率需采购后实测
- 报告：`.project-log/docs/printer-research-prc-2026-09-11.md`

## 2026-09-11 工单列表排序与工作台过滤

- 用户反馈：已撤销工单仍与有效工单混在一起，工作台最近工单也不应显示已撤销工单
- 工单管理：后端工单列表改为已撤销状态固定排到最后，非撤销工单仍按创建时间倒序；导出和后续平板端调用同一接口也保持该顺序
- 工作台：最近工单列表过滤掉已撤销工单，状态筛选去掉“已撤销”选项，避免出现互相矛盾的筛选
- 验证：后端全部测试通过（含撤销工单置底用例）；`npm run build` 通过；真实 API 返回顺序为未撤销工单在前、已撤销工单在后

## 2026-09-11 今日工单统计排除已撤销

- 用户反馈：共有 3 个工单，其中 2 个已撤销，但工作台“今日工单”仍显示 3
- 根因：前端 `getDashboard` 直接使用 `workOrders.length` 统计今日工单，没有排除 `cancelled` 状态
- 修复：新增 `activeWorkOrderCount`，只统计未撤销工单；已撤销工单仍保留在工单列表中，便于追溯
- 验证：`npm run build` 通过；前端开发服务继续返回 200

## 2026-09-11 SQLite readonly 数据库修复

- 用户反馈：配方停用操作报 `Internal Server Error`
- 根因：在准备提交时通过 `git restore` 还原了已跟踪的 `pc/data/milk_weigh.sqlite3`，但当时后端服务仍持有旧 SQLAlchemy/SQLite 连接，后续写入报 `attempt to write a readonly database`
- 修复：重启后端服务，让 SQLAlchemy 重新打开数据库；通过写锁和 `PATCH /api/v1/master-data/products/6/active` 同状态写入验证，数据库恢复正常可写
- 注意：后续不要在运行后端的期间还原/替换 SQLite 数据库文件；如确需还原，先停止后端服务

## 2026-09-11 配方状态切换下拉列表

- 用户反馈：点击配方状态右侧小箭头不应直接切换，应先弹出包含“启用/停用”的下拉列表，选中状态后再执行切换
- 实现：右侧箭头改为展开下拉菜单，菜单内展示“启用”“停用”两个选项；选择当前状态不会重复调用接口；选择停用仍保留确认提示
- 交互细节：箭头在展开时旋转为向下方向，点击菜单外自动关闭；菜单沿用现有筛选下拉的视觉样式，停用选项使用红色
- 验证：`npm run build` 通过；前端开发服务继续返回 200

## 2026-09-11 配方删除与状态切换交互优化

- 用户反馈两项 PC 端交互问题：配方创建后无法删除；配方状态栏暴露“停用/启用”按钮，希望只显示状态并用右侧小箭头切换
- 配方删除：新增 `DELETE /api/v1/master-data/products/{product_id}`，删除产品、配方、配方明细和产品图片；前端在“编辑产品及辅料配方”弹窗底部增加“删除配方”按钮，点击后二次确认，删除成功自动关闭弹窗并刷新列表
- 状态切换：移除配方卡片底部暴露的“停用/启用”按钮；状态栏只显示“启用/停用”，启用绿色、停用红色，右侧箭头改为可点击状态切换入口，停用前仍确认
- 详情弹窗同步修复：配方详情中的配方状态不再写死为“启用”，改为读取真实 `recipe_enabled`
- 验证：后端全部测试通过（含 `test_product_recipe_can_be_deleted`）；`npm run build` 通过；后端已重启，健康检查与前端均返回 200

## 2026-09-09 产品配方编辑与用量输入修复

- 用户反馈两个 PC 端问题：已创建配方不能编辑；创建配方时每吨用量输入 `5` 提示无效
- 根因：用量输入设置了 `step="0.01"` 与 `min="0.001"`，浏览器原生校验以 min 为步进基准，导致 `5` 无法通过
- 修复：用量输入改为 `step="any"`，任意大于 `0.001` 的正数均通过校验
- 实现产品配方编辑：新增 `PUT /api/v1/master-data/products/{product_id}`，前端复用创建弹窗进入编辑模式，支持修改名称、辅料清单、每吨用量和可选图片
- 修复编辑保存的唯一约束冲突：更新配方时先清空并 flush 旧配方明细，再写入新明细
- 验证：后端 24 项测试通过（含 `test_product_recipe_can_be_edited`）；`npm run build` 通过；后端已重启，健康检查与前端均返回 200

## 2026-09-09 产品配方启用/停用与新增账号修复

- 用户反馈：配方状态全部写死为“启用”，无法修改；账号管理创建账号时显示“请求失败”
- 配方状态：新增 `PATCH /api/v1/master-data/products/{id}/active`，同时切换产品与配方的启用状态；前端配方卡片显示真实状态（启用的绿点/停用灰点），底部增加“停用/启用”按钮，停用后不能用于新建工单
- 创建账号修复：新建账号弹窗默认密码由 `123456`（6 位，不满足后端至少 8 位）改为 `12345678`；前端 `parseError` 增加对 pydantic 数组 `detail` 的处理，校验失败时显示具体原因而非笼统“请求失败”
- 配方编辑按钮布局：将编辑按钮改为带文字的“编辑”按钮，并与详情箭头放入统一右侧操作区，避免被 `margin-left:auto` 挤到中间
- 验证：后端 25 项测试通过（含配方启停用例）；`npm run build` 通过；后端已重启，健康检查与前端返回 200；真实 HTTP 启停接口验证通过

## 2026-09-09 REQ-001 已批准

- 用户确认第一版按照 `REQ-001` 基线执行
- `REQ-001` 已更新为 `active` / `approved`
- 生命周期已切到 `solution-research`
- 下一步开始技术方案讨论，不做实现

## 2026-09-09 技术方案讨论启动

- 已登记 RES-002：后端、数据库、平板 App、二维码、串口读秤、标签打印和部署候选
- 技术讨论重点：技术栈、数据库、Android App 形态、串口适配、打印适配和现场部署方式
- 当前建议：先搭最小可运行测试版，电子秤串口和标签打印机作为独立 spike 验证

## 2026-09-09 第一版技术边界确认

- 第一版不连接电子秤，称重结果由平板手动输入并标记 manual
- Q-004 已标记为 answered，RES-001 标记为 superseded
- REQ-001 更新为 v2，DEC-023 已记录

## 2026-09-09 第一版技术栈确认

- 后端：Python FastAPI，部署在 Windows 10/11
- 数据库：SQLite 第一版
- 平板：Android 原生 Kotlin APK
- 电脑端：浏览器管理端，计划用 React + Vite 构建静态文件并由 FastAPI 托管
- DEC-024 已记录；浏览器前端框架如需调整可在进入开发前低成本修改

## 2026-09-09 Windows 部署方式讨论

- 用户确认 React + Vite 浏览器管理端方向
- 用户询问电脑端软件用 Docker 还是安装包
- 已登记 RES-002 部署候选和 DEC-025 建议：Windows 安装包 + 后台服务优先
- 用户已确认安装包策略，DEC-025 已标记为 active/approved

## 2026-09-09 操作员称量工作流梳理

- 已梳理第一版操作员完整路径：登录平板 -> 获取/创建工单 -> 按顺序处理辅料步骤 -> 类型确认 -> 手动重量确认 -> 提交完成
- 正常放行路径：扫码匹配类型 -> 手动输入重量 -> 保存证据
- 异常路径：无码/标签异常拍照申请 -> 后台审批 -> 审批通过后称重
- 文档位置：`.project-log/docs/operator-weighing-workflow.md`

## 2026-09-09 称重读数照片强证据确认

- 用户确认第一版手动称重不能只记录数字，还需要拍摄电子秤读数照片
- 数字重量和电子秤读数照片必须一起保存，任缺其一不能完成对应辅料步骤
- REQ-001 已更新为 v3，DEC-026 已记录

## 2026-09-09 架构设计前待确认点整理

- 已整理称重口径、主数据/工单、账号审批、平板网络、Windows 部署、标签打印等待确认项
- 每项已给出建议默认值，等待用户逐项确认或调整
- 文档位置：`.project-log/docs/solution-research-input-questions.md`

## 2026-09-09 架构输入默认确认

- 用户同意全部建议默认：净重 kg、读数照片包含材料和读数、配方快照、工单可暂停续作、Excel 不覆盖、管理员审批、无离线队列
- 平板按最新 Android 系统适配并支持 APK
- 后端默认 Windows 11，Windows 支持安装软件
- 安装包内置自动备份数据库
- REQ-001 已更新为 v4，DEC-027 已记录

## 2026-09-09 架构设计启动

- 生命周期已进入 `architecture-decision`
- 已创建 `ARCH-001` 草稿，覆盖组件、接口、数据流、状态并发、失败路径、安全、可观测、部署和兼容
- 下一步对架构草稿进行评审和细化

## 2026-09-09 Q-022 已确认

- 目标/任务：确认标签打印时实际生产日期如何录入
- 已完成内容：
  - 第一版不录入和管理实际生产日期
  - 二维码标签日期以标签打印日期 printedAt 为准
  - 实际生产日期和到期日期校验暂时不做，后续需要批次/有效期管理时再增强
  - DEC-016 已标记为被 DEC-022 取代，BL-LABEL-001/BL-LABEL-002 已更新

## 2026-09-09 Q-021 已确认

- 目标/任务：确认标签打印设备与后台打印流程
- 已完成内容：
  - 标签打印机需要新购并配合系统接入，具体型号延后确定
  - 后台打印界面选择辅料、打印张数、点击打印
  - 系统自动记录当前打印时间并发送打印指令，操作员拿标签粘贴
  - 已新增 DEC-021 和 BL-LABEL-002
- 下一步：
  - 确认 Q-022 打印时生产日期如何录入

## 2026-09-09 Q-019 已确认

- 目标/任务：确认后端主机、局域网和标签打印设备边界
- 已完成内容：
  - 后端和数据库部署在现场可用电脑上
  - 现场已有可用 WiFi，平板与后端同一局域网访问
  - 系统不依赖互联网
  - 已新增 DEC-020 和 BL-DEVICE-002
- 下一步：
  - 确认 Q-021 标签打印设备

## 2026-09-09 Q-020 已确认

- 目标/任务：确认辅料主数据字段范围和英文名称必填规则
- 已完成内容：
  - materialId 系统自动生成且不可变
  - materialCode 公司内部代号必填
  - 中文名称必填，英文名称选填
  - 默认保质期必填，enabled 默认启用
  - 包装图片支持多张，英文名称不作为身份匹配字段
  - 已新增 DEC-019，并更新 BL-DATA-003
- 下一步：
  - 确认 Q-019 后端主机、局域网和标签打印设备边界

## 2026-09-09 辅料与产品配方主数据录入规则已确认

- 目标/任务：记录辅料和产品配方在电脑端、平板端的录入规则
- 已完成内容：
  - 辅料信息包括中文名称、英文名称/代号、公司内部代号、默认保质期和多张包装图片
  - 辅料录入支持电脑端选择本地图片，平板端选择相册图片或直接拍摄
  - 辅料和产品配方录入仅管理员可操作；APP 区分普通操作员和管理员，管理功能只对管理员开放
  - 产品配方从现有辅料库选择辅料并填写每吨用量；缺少辅料时先补录辅料再录产品配方
  - 已新增 DEC-018、BL-DATA-003、BL-DATA-004
- 下一步：
  - 确认 Q-020 辅料字段范围和英文名称必填规则

## 2026-09-09 Q-018 已确认

- 目标/任务：确认产品、配方和辅料主数据的初始化与维护方式
- 已完成内容：
  - 第一版支持后台手工录入，也支持 Excel 模板导入/导出
  - 导入/导出覆盖产品、配方和辅料主数据，模板字段与后台手工维护字段一致
  - 导入前校验必填项、重复编码和配方引用关系，失败行给出原因且不覆盖有效数据
  - 已新增 DEC-017，并更新 BL-DATA-001
- 下一步：
  - 确认 Q-019 后端主机、局域网和标签打印设备边界

## 2026-09-09 Q-017 已确认

- 目标/任务：确认自制二维码标签的生成、打印、粘贴复核流程
- 已完成内容：
  - 标签生成/打印放在电脑后台，由管理员操作
  - 打印时录入实际生产日期，保质期从辅料主数据自动带出，到期日期按“生产日期 + 保质期”计算
  - 每次打印生成独立 labelId，同一种辅料可共用 materialId
  - 标签状态保留 ACTIVE/VOID，粘贴后扫码复核
  - 标签未生成、污损或扫描异常仍走拍照申请审批
  - 已新增 DEC-016，并将 BL-LABEL-001 更新为用户确认
- 下一步：
  - 确认 Q-018 主数据初始化与维护方式

## 2026-09-09 外部自制二维码方案已评估

- 目标/任务：评估用户提供的《辅料自制二维码方案设计说明》是否适合第一版
- 已完成内容：
  - 核心方向适合：不可变 materialId、后台主数据为真实数据源、自制二维码保存标签快照、labelId 标签记录、扫码按 materialId 匹配、异常走拍照审批
  - 已确认并作为第一版基线：公司自制二维码为主，供应商原码不作核心身份
  - 外部文档中的打印权限、生产日期录入、独立 labelId、ACTIVE/VOID、粘贴后扫码复核等细节属于候选，未直接作为已确认需求
  - 已新增 Q-017，下一步向客户确认标签生成、打印、粘贴复核流程
- 验证与限制：
  - 项目日志校验通过；具体标签字段和流程仍需客户逐项确认

## 2026-09-09 Q-014 背景文档已生成

- 目标/任务：生成可用于外部咨询的二维码映射分析 Brief
- 已完成内容：
  - 已生成 `.project-log/business-logic/q014-qr-mapping-brief.md`
  - Brief 包含项目背景、混合识别方案、现场包装/码型事实、样例收集要求和映射表模板
  - 用户可连同二维码图片发给 GPT，重点确认现有码是否能稳定映射到内部代号
- 下一步：
  - 根据 GPT 分析结果回答 Q-014

## 2026-09-09 Q-015 已确认

- 目标/任务：确定第一版辅料类型识别方案
- 已完成内容：
  - 第一版采用方案 C 混合方案
  - 能有效扫描的现有码映射到内部代号后自动匹配
  - 无码、无效码或未映射码走拍照 + 手动选择 + 后台审批
  - 自制二维码贴纸作为后续增强，不在第一版强制铺开
- 下一步：
  - 确认 Q-014 现有码如何映射到辅料内部代号

## 2026-09-09 辅料识别现状反馈

- 目标/任务：记录现场包装、码型和内部代号现状，并确定第一版辅料识别方案
- 已完成内容：
  - 辅料包装不统一：塑料桶、纸箱、蛇皮袋
  - 有码/无码混合；部分条形码扫描无效
  - 二维码扫描结果多为供应商产品溯源信息
  - 公司内部使用 A1/E2 类辅料代号，不直接使用辅料名称
  - 新增 Q-015：贴码、现有码加拍照申请、混合方案待确认
- 下一步：
  - 等待用户选择第一版识别方案

## 2026-09-08 Q-005 已确认

- 目标/任务：确认第一版无码辅料的申请、放行与称重时序
- 已完成内容：
  - Q-005 已确认：先审批后称重
  - 测试版流程：平板端拍照申请，电脑端马上审批，审批通过后才能称重
  - 测试版人员分工：一个人拿平板，一个人在电脑前审批
  - 真实生产版本的无码放行策略暂不定义，作为后续确认项
- 下一步：
  - 确认 Q-006 重量正确判定规则

## 2026-09-08 Q-006 已确认

- 目标/任务：确认称重完成时的重量正确判定规则
- 已完成内容：
  - Q-006 已确认：按辅料/配方配置允差
  - 测试版先使用统一默认值，默认建议为取目标重量 5g 与 1% 中的较大绝对偏差
  - 允差、稳定条件等参数放在设置界面，允许用户自行调整
- 下一步：
  - 确认 Q-007 电子秤异常时的手动输入规则

## 2026-09-08 Q-007 已确认

- 目标/任务：确认电子秤离线或读数异常时如何确认重量
- 已完成内容：
  - Q-007 已确认：允许手动输入，测试版不做电脑端审批
  - 手动输入必须标记为“手动读数”，并记录操作员、时间和读数
  - 正式生产版本是否需要审批或二次确认，后续再确认
- 下一步：
  - 确认 Q-008 同一工单与多台平板的关系

## 2026-09-08 Q-008 工单绑定流程已确认

- 目标/任务：确认工单创建、绑定账号与接管规则
- 已完成内容：
  - 平板是主要业务操作端，电脑后台主要做审批、数据管理和工单管理
  - 操作员在平板创建工单，后台审批通过后绑定到该账号，只有该账号可操作
  - 平板可查看其他工单状态和绑定账号，可申请接管，后台审批后重新绑定
  - 电脑后台可以创建工单并选择绑定哪个账号
- 下一步：
  - 确认 Q-009 工单撤销/作废规则

## 2026-09-08 Q-009 工单完成与撤销规则已确认

- 目标/任务：确认工单完成条件和撤销/删除规则
- 已完成内容：
  - 工单必须按顺序完成全部辅料步骤后才能提交完成
  - 每个辅料保留二维码/图片证据和实际重量记录
  - 平板端可申请撤销/删除，后台审批通过后生效
  - 后台也可以直接撤销/删除工单
- 下一步：
  - 确认 Q-010 撤销/删除后是否保留历史记录

## 2026-09-08 Q-010 删除保留记录已确认

- 目标/任务：确认撤销/删除后的记录保留策略
- 已完成内容：
  - 用户确认删除也要保留记录
  - 第一版采用撤销/作废和逻辑删除，不物理删除工单和证据
- 下一步：
  - 确认 Q-011 电脑后台账号和权限如何划分

## 2026-09-08 Q-011 账号权限已确认

- 目标/任务：确认电脑后台账号和权限划分
- 已完成内容：
  - 平板端创建的账号均为普通操作员账号
  - 电脑后台账号属于管理员账号，为预置账号
  - 后台可以创建和管理普通账号
- 下一步：
  - 确认 Q-012 称重记录和证据保存多久

## 2026-09-08 Q-012 长期保存已确认

- 目标/任务：确认称重记录和证据保存时长
- 已完成内容：
  - 用户确认称重记录和证据长期保存
  - 第一版不限制保存时长，不物理删除
- 下一步：
  - 确认 Q-013 配方中辅料重量如何配置和换算

## 2026-09-08 Q-013 按吨配方已确认

- 目标/任务：确认配方中辅料重量如何配置和换算
- 已完成内容：
  - 配方按“每吨成品需要多少辅料”录入
  - 系统按目标生产重量除以 1000kg 等比换算应称重量
- 下一步：
  - 确认 Q-014 二维码与辅料主数据如何对应

## 2026-09-08 继续业务澄清

- 目标/任务：在 Q-004 保持开放的前提下，继续收敛第一版业务逻辑
- 已完成内容：
  - 新增 Q-005：第一版无二维码辅料的申请、放行与称重时序
  - 明确后续澄清顺序：无码辅料流程 -> 重量允差/电子秤异常 -> 审批角色与留痕 -> 并发规则
- 下一步：
  - 等待用户回答 Q-005，再更新状态流和业务原子

## 2026-09-08 阶段纠正

- 情况：在 business-clarification 阶段误开始创建后端代码文件
- 处理：已删除误创建文件，仓库恢复为仅有 README、AGENTS 和项目日志
- 状态：阶段仍为 business-clarification，不进入实现
- 下一步：先完成需求基线和方案确认，再写代码

## 2026-09-08 辅料确认方案与第一版范围

- 目标/任务：落实辅料类型确认的最终方案和第一版范围
- 已完成内容：
  - 确认最终目标：二维码自动识别；无二维码拍照并由后端模型自动对比包装图片；识别不符可申请放行并附理由/证据
  - 确认第一版范围：二维码识别 + 拍照申请/审批；模型识别后续再做
  - 确认平板以安装包交付，暂按安卓准备
  - 确认后端电脑作为串口读取和局域网服务主机，平板通过电脑读取电子秤
- 下一步：
  - 进入 MVP 技术方案和项目搭建

## 2026-09-08 系统架构确认

- 目标/任务：评审用户提出的电脑后端 + 平板 App 局域网架构
- 已完成内容：
  - 确认架构：电脑端数据库和后端服务，平板 App 通过局域网访问
  - 确认多平板并发、操作员账号身份、工单/产品/步骤流程
  - 确认称重自动读电子秤，失败时手动输入
- 评审结论：
  - 架构方向合理，建议作为 MVP 基线
  - 需要进一步明确：拍照确认是自动识别还是拍照留档+手动确认；后端电脑是否直连电子秤串口；平板 App 采用 PWA/原生形式，因为局域网 HTTP 下浏览器摄像头权限会受限制
- 下一步：
  - 等用户确认上述技术边界后进入 solution-research/architecture

## 2026-09-08 开发路线

- 目标/任务：客户确认 Q-004 保持开放，并暂按“电子秤串口数据可读取”推进后续开发
- 已完成内容：
  - 将“串口可读”记录为暂定开发假设，不关闭 Q-004
  - 明确重量读取层需要支持串口、模拟、手动确认三类实现，避免硬件未确认阻塞开发
  - 确认仓库当前只有 README.md 和项目日志，应用代码尚未开始
- 建议下一步：
  1. 定串口读取主机和部署边界（推荐 Q1S 或本地小主机跑后端，平板通过局域网 PWA 访问）
  2. 做 MVP 技术选型与架构方案
  3. 先实现主数据、配方计算、称重流程，再接真实串口
- 验证与限制：
  - 真实串口联动仍待现场验证；Q-004 保持 open
- 下一步：
  - 等待用户确认“串口读取主机和部署方式”这一关键技术边界

## 2026-09-08 电子秤硬件查证

- 目标/任务：查询客户提供的 ENTOAM 映拓 Qy-C126 电子秤及 Q1S 收款机是否支持外部连接读取实时重量
- 已完成内容：
  - 记录客户硬件：ENTOAM 映拓 Qy-C126，配套收款机 Q1S，主板 Gm-J6412D16-V2E
  - 找到映拓计价秤公开串口对接文档：通讯秤、默认 COM2、约 50ms 取重间隔、映拓主动通讯协议
  - 找到聚商荟帮助文档：C650 映拓秤工具通过串口 COM2 打开端口并设置主动协议
  - 找到同系列 QY-A126 第三方协议分析：23 字节帧可从串口实时读取重量，含稳定标志、符号位、克重和 CRC
  - 未找到 Qy-C126 官方手册；未找到 Gm-J6412D16-V2E 公开规格
- 查证结论：
  - 疑似支持外部串口实时取重，但 Qy-C126 实物数据口、固件协议状态和 Q1S COM 端口仍需现场核实
- 重要决策：无；Q-004 保持开放
- 验证与限制：
  - 资料为厂商/第三方收银软件文档或第三方逆向分析，不是 Qy-C126 官方规格确认
  - 下一步需要客户提供实物端口照片和厂商协议，再做可信确认
- 参考资料：
  - https://tddoc.td365.com.cn/docs/tdgj/yingt
  - https://tddoc.td365.com.cn/docs/tdgj/cpzcxh
  - https://help-jsh.paidui.com/008c/e14f
  - https://www.cnblogs.com/minuhy/p/20078724
- 下一步：
  - 向客户说明查证结果并提供需要现场确认的问题

## 2026-09-08 业务澄清会话（第一轮）

- 目标/任务：进入 business-clarification，澄清辅料称重防错系统业务逻辑
- 已完成内容：
  - 启动 RUN-20260908-070218-c4bc363a
  - 记录客户首轮描述：产品、配方、目标重量、辅料扫码/拍照、电子秤称重
  - Q-001 已确认：独立系统，不连接金蝶
  - Q-002 已确认：用户自行维护产品、配方和辅料主数据
  - Q-003 已确认：首选平板，扫码枪为次选
  - 建立 Q-004：电子秤重量数据如何进入系统
  - 将 Q-004 拆成三项客户确认问题：电子秤连接能力、连接必要性、无连接方案
  - 连接 GitHub 远程仓库 `origin`，并 fetch `main` 与 `milk_preview`
- 重要决策：
  - DEC-001：独立系统，不连接金蝶
  - DEC-002：主数据由用户在本系统内维护
  - DEC-003：平板优先，扫码枪次选
- 验证与限制：
  - `validate_project.py` 已通过
- 下一步：
  - 等待客户回答 Q-004 的三项子问题并更新事实地图

## 2026-09-08 初始化会话

- 目标/任务：初始化辅料称重防错系统项目
- 已完成内容：
  - 执行 `git init`，初始化个人 Git 仓库
  - 调用 `init_project_agents.py` 创建 `.project-log/`
  - 创建根 `AGENTS.md`，写入通用开发规则与“仓库类型：个人仓库”
- 重要决策：
  - 用户确认使用个人仓库
- 验证与限制：
  - `loopctl restore` 正常，phase=business-intent，native_goal=unbound
  - `validate_project.py` 已通过
- 下一步：
  - 澄清业务目标并进入 business-clarification

<!--
会话区块按日期倒序向下追加；超过约 50-100 KB 或约 10 条时归档到
`.project-log/docs/archive/`，归档文件按日期可检索。
-->
